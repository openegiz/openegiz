{{- if and .Values.hono.enabled .Values.persistentVolumes.hono }}

apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-openegiz-hono
spec:
  accessModes: 
   - ReadWriteOnce
  capacity:
    storage: 1Mi
  hostPath:
    path: /openegiz/hono
    type: Directory

{{- end }}